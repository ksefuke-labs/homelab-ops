apiVersion: v1
kind: Service
metadata:
  labels:
    app: calibre
  name: calibre
  namespace: calibre
spec:
  ports:
  - port: 8083
    protocol: TCP
    targetPort: 8083
  selector:
    app: calibre
  type: ClusterIP
