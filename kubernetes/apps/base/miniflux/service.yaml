apiVersion: v1
kind: Service
metadata:
  labels:
    app: miniflux
  name: miniflux
  namespace: miniflux
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: miniflux
  type: ClusterIP
